// Bellbrook Darts Club — Tipping Module Supabase connection config.
//
// Fill these in after creating your free Supabase project:
//   1. supabase.com → New project (free tier, no card needed)
//   2. Project Settings → API → copy "Project URL" and the "anon public" key
//   3. Paste them below. These are safe to publish in client-side code —
//      Row Level Security in schema.sql is what actually protects the data,
//      not secrecy of this key.
//   4. Run schema.sql in the SQL Editor before first use.

const SUPABASE_URL = 'YOUR_SUPABASE_PROJECT_URL'; // e.g. https://abcdefgh.supabase.co
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
