# Club match records

Match records published from the scoreboard live here as JSON files
(one per game). The Club History page (`history.html`) reads this
folder to build the player ladder and match list everyone can see.

To publish a game: finish it on the scoreboard, then tap
**Publish to Club History** on the winner screen and commit the
pre-filled file (needs the club GitHub login).
